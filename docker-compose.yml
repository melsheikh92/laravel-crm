version: '3.8'

services:
  # MySQL Database Service
  mysql:
    image: mysql:8.0
    container_name: provensuccess_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: provensuccess
      MYSQL_USER: provensuccess_user
      MYSQL_PASSWORD: provensuccess_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - provensuccess_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Laravel Application Service
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: provensuccess_app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
      - ./storage:/var/www/html/storage
      - ./bootstrap/cache:/var/www/html/bootstrap/cache
    ports:
      - "8000:8000"
    networks:
      - provensuccess_network
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_DATABASE=provensuccess
      - DB_USERNAME=provensuccess_user
      - DB_PASSWORD=provensuccess_password
      - APP_ENV=local
      - APP_DEBUG=true
      - APP_URL=http://localhost:8000

  # Nginx Web Server (Optional - for production-like setup)
  nginx:
    image: nginx:alpine
    container_name: provensuccess_nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - provensuccess_network
    depends_on:
      - app

volumes:
  mysql_data:
    driver: local

networks:
  provensuccess_network:
    driver: bridge

